import { useState, useRef, type ChangeEvent } from 'react';
import UploadPage from './pages/scrum-23_tela_upload';

export default function App() {
    return (
        <main>
            <UploadPage />
        </main>
    );
}